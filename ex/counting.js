// Count to ten

// We need to specify

// Where we should START counting?
// Where is the FINISH line?
// What the STEP SIZE we should use?

//        START      FINISH      STEP SIZE
for (let count = 1; count < 11; count += 1) {
  console.log(count);
}
